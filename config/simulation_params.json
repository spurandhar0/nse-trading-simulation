{
  "_comment": "NSE Trading Simulation Parameters — edit values here, no code changes needed",

  "signal_start_date": "2025-01-01",
  "signal_end_date":   "2025-12-31",

  "filter": {
    "_comment_filter": "Filter combos: all combinations of these arrays are tested. pct_min < pct_max and ath_min < ath_max (both negative). Invalid combos auto-skipped.",
    "days_back": [5],
    "pct_min":   [-0.10],
    "pct_max":   [-0.05],
    "ath_min":   [-0.60],
    "ath_max":   [-0.30]
  },

  "trade": {
    "_comment_trade": "Trade combos: all combinations = MaxBuys(4) x BuyDrop(4) x Target(5) x Stoploss(5) x MaxDuration(5) = 2000 rows",
    "max_buys":    [1, 2, 3, 4],
    "buy_drop":    [0.05, 0.10, 0.15, 0.20],
    "target":      [0.10, 0.15, 0.20, 0.25, 0.30],
    "stoploss":    [0.05, 0.10, 0.15, 0.20, 0.25],
    "max_duration":[20, 30, 40, 50, 60]
  },

  "investment_per_buy":       10000,
  "pending_window_days":      30,
  "force_exit_calendar_days": 90,
  "max_rows_per_sheet":       25000,

  "_comment_full_test": "For full parameter sweep (all ranges from VBA modConfig), change filter to:",
  "_full_test_filter_example": {
    "days_back": [5, 10, 15, 20],
    "pct_min":   [-0.15, -0.12, -0.09, -0.06, -0.03],
    "pct_max":   [-0.10, -0.07, -0.04, -0.01],
    "ath_min":   [-0.70, -0.60, -0.50, -0.40, -0.30, -0.20],
    "ath_max":   [-0.60, -0.50, -0.40, -0.30, -0.20, -0.10]
  }
}
